# Security

setupはroot/sudoを要求せず、`$HOME`以下だけを変更します。SSHはTermux側で明示的に `sshd -p 8022` を起動した時だけ待ち受けます。0.0.0.0への公開範囲は端末のWi-Fi設定に依存するため、信頼できるLANでのみ使ってください。

APIキー・パスワード・トークンをログへ出さず、リポジトリへ保存しません。署名鍵は端末内にのみ置きます。curl|bashは公開tag/commitを固定し、実行前にsetup.shを確認してください。
